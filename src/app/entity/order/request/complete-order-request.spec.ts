import { CompleteOrderRequest } from './complete-order-request';

describe('CompleteOrderRequest', () => {
  it('should create an instance', () => {
    expect(new CompleteOrderRequest()).toBeTruthy();
  });
});
